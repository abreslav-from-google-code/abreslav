program a;
var
  a, b : Integer;
  c : Real;
begin
  |a := a {{
  	assertNull(unit.a);
  }};
  1:
  | a := 1 
  !{{$a == 1 /* 0 */}};
  1:
  if a > 0 then
    1:
    |a := a + 1 !{{$a == 2}};
  1:
  |while a > 0 do
    2:a := a - 1
  !{{$a == 0}};
  1:
  if a > 0 then
    0:|a := a + 1 !{{$a == 1 }}
  else 
  	1:|a := a + -1 !{{$a == -1}};
end.